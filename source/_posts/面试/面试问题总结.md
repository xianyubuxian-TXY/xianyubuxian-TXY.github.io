---
title: 面试问题总结
date: 2026-03-05 11:05:11
tags:
	- 笔记
categories:
	- 面试
	- 面试问题总结
---

# 岗位要求
## 1.腾讯后端开发
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260307223514017.png)

# 一、项目相关问题
## 1.集群聊天服务器
### （1）性能指标是如何测试得到的
#### <1>5W+ 并发长连接，连接稳定性 100% 是如何测试出来的？
- 因为我的项目是自定义 TCP+JSON 协议，所以我并**没有使用wrk/ab等通用工具**，而是在AI辅助下编写测试脚脚本进行性能测试
- 为达到 **5w+并发**，需要进行**负载均衡** 和 **性能调优**

##### 1）负载均衡方面
- 使用**Nginx实现负载均衡**：因为Nginx反向代理时，需要同时与客户端、目标服务器建立TCP连接，为了达到5w+并发连接，需要进行一定的Nginx配置
    - 首先：就是需要将“worker进程文件描述符限制”调大，避免建立TCP连接时没有文件描述符可用
        - 参考：{% post_link "C++/C++后端开发常用库/nginx的使用" "nginx的使用" %}
    - 其次：“事件模型”选择 epoll模型、设置“允许一次接受多个连接（worker_connections）”、“最少连接负载均衡策略(least_conn)”、“长连接超时（proxy_timeout）”等

##### 2）性能调优方面
- 主要包括“系统性能调优”、池化技术调优
- **系统性能调优**：
    - 通过**ulimit设置文件描述符限制**到200000，避免多并发连接时文件描述符不足
    - 进行**TCP参数优化**：
```bash
sudo sysctl -w net.core.somaxconn=65535                        # 监听队列最大长度
sudo sysctl -w net.core.netdev_max_backlog=65535            # 网卡接收队列最大长度
sudo sysctl -w net.ipv4.tcp_max_syn_backlog=65535            # 半连接队列最大长度（SYN_RECV状态）
sudo sysctl -w net.ipv4.tcp_fin_timeout=10                    # FIN_WAIT_2 状态超时时间（秒）
sudo sysctl -w net.ipv4.tcp_tw_reuse=1                        # 允许复用 TIME_WAIT 状态的连接
sudo sysctl -w net.ipv4.ip_local_port_range="1024 65535"    # 本地端口分配范围
```
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260308221210660.png)
- **池化技术调优**：
    - 将muduo线程池大小扩大一倍
    - 在配置文件中，将mysql连接池大小增加到150，增加最小空闲连接等
    - 增加mysql最大连接数，适配mysql连接池大小

##### 3）"连接稳定性 100%" 如何得出
```bash
稳定性 = 成功保持连接的数量 / 目标建立连接的数量

具体做法：
1. max_connection_test 建立 5W+ 个 TCP 长连接
2. 每隔一段时间（比如5秒）发一次心跳包
3. 统计在整个测试周期内（比如60秒）
   连接没有被服务器主动断开的比例

结果：5W+ 连接在测试周期内全部保持稳定，稳定性 100%
```

#### <2>登录 QPS 480+，消息延迟 P99 < 30ms
```bash
面试官：登录 QPS 480 和 P99 < 30ms 是怎么测出来的？

你：
用自研的 C++ 压测客户端，模拟真实的登录和消息收发流程。

登录 QPS 方面：
100 并发持续发送登录请求，统计单位时间内成功次数。

P99 延迟方面：
10 个客户端各发 1000 条消息，记录每条消息从发出到
收到服务器转发的时间戳，共 1000 个延迟样本。
排序后取第 99% 分位，结果是 28ms，低于 30ms。

关注 P99 而不是平均值，是因为平均值会掩盖
高并发下的尾部延迟，P99 更能反映真实服务质量。
```


# 二、Linux相关问题
## 1.操作系统相关问题

### （1）Linux进程虚拟地址空间
```cpp
    高地址 0xFFFFFFFF (32位) / 0x7FFFFFFFFFFF (64位)
    ┌─────────────────────────────┐
    │         内核空间             │  ← 用户不可访问
    ├─────────────────────────────┤ 
    │          栈 (Stack)         │  ← 向下增长 ↓
    │             ↓               │     局部变量、函数参数、返回地址
    │            ...              │
    ├─────────────────────────────┤
    │                             │
    │      （未分配区域）           │
    │                             │
    ├─────────────────────────────┤
    │             ↑               │
    │          堆 (Heap)          │  ← 向上增长 ↑
    │                             │     malloc/new 分配
    ├─────────────────────────────┤
    │                             │
    │      可读写数据段            │  ← .data（已初始化全局/静态变量）
    │      (.data + .bss)         │     .bss（未初始化全局/静态变量）
    │                             │
    ├─────────────────────────────┤
    │                             │
    │        只读段                │  ← .rodata（只读数据、字符串常量）
    │    (.text + .rodata)        │     .text（代码段、机器指令）
    │                             │
    ├─────────────────────────────┤
    │         保留区               │  ← 防止空指针访问
    └─────────────────────────────┘
    低地址 0x00000000
```

### （2）高级语言程序文件 ——> 可执行文件
要经历：**预处理、编译、汇编、链接** 四个阶段
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310151747560.png)
- 预处理：头文件展开
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310151900753.png)

### （3）可执行文件的装入与执行全过程（重点）
- 系统中要运行用户程序，就必须先将其**装入内存中**
- 需要明确：可执行文件内部已经是**二进制数据了**
```hex
# ELF文件头 + main函数入口片段
7F 45 4C 46 02 01 01 00  00 00 00 00 00 00 00 00  |.ELF............|
02 00 3E 00 01 00 00 00  78 10 40 00 00 00 00 00  |..>.....x.@.....|
40 00 00 00 00 00 00 00  F0 21 00 00 00 00 00 00  |@.......!.......|
00 00 00 00 40 00 38 00  09 00 40 00 1E 00 1D 00  |....@.8...@.....|
55 48 89 E5 48 83 EC 20  48 C7 45 F8 00 00 00 00  |UH..H.. H..E.....|
48 C7 45 F0 00 00 00 00  48 89 7D E8 48 89 75 E0  |H..E.....H.}..H.u.|
31 C0 89 C2 0F 05 48 31  C0 48 83 C4 20 5D C3     |1.....H1.H.. ].|
```

#### <1>二进制指令执行过程（基础知识）
- 指令执行过程：**取指、译码、执行、访存、写回**（流水线加速）

##### 1）取指
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310153733766.png)

##### 2）译码
- 把取指阶段拿到的「二进制指令码」翻译成CPU能理解的"操作命令"：
    - 指令的二进制编码包含两部分核心信息：**操作码（做什么） + 操作数（对谁做）**；
    - 译码阶段会解析出：要执行的操作（比如加法、减法、移动数据）、操作数的来源（比如寄存器、内存地址、立即数）。

##### 3）执行
- 执行译码阶段解析出的**具体操作**，是指令的"计算核心阶段"：
    - 如果是算术运算（加减乘除）、逻辑运算（与或非）：由运算器完成计算；
    - 如果是数据移动（比如把A寄存器的值移到B寄存器）：直接在寄存器间传输；
    - 如果是控制操作（比如跳转）：修改程序计数器（PC）的值，改变指令执行顺序。

##### 4）访存
- 与内存交互：读取**内存中**的数据，或**把执行结果写入内存**（**不是所有指令都需要这一步**）：
    - 读内存：比如指令要求"把内存地址0x100的值加载到寄存器"，执行阶段算出地址后，访存阶段从内存取数；
    - 写内存：比如指令要求"把ALU的计算结果存入内存地址0x200"，访存阶段完成写入；
    - 无内存操作的指令（比如寄存器间的加法）：这一步会直接跳过，仅做"空操作"。
                                                             
##### 5）写回
- 把指令执行的最终结果**写回CPU的寄存器**（或特殊存储位置），供后续指令使用：
    - 如果是算术运算：把ALU的结果写入目标寄存器；
    - 如果是读内存：把从内存取出的数据写入目标寄存器；
    - 写回是指令执行的"收尾"，确保结果被保存，后续指令能调用。

#### <2>ELF可执行目标文件 格式解析
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310155459988.png)
- 先明确：ELF可执行文件，本质是一个文件，存在磁盘中
- **ELF头**： ELF头中的e_entry给出了程序的**起始虚拟地址（入口点）**（通常是_start,而不是main），即**执行程序时第一条指令的地址**
- **程序头表**：在后面我们会看到，ELF可执行文件的“只读（代码）段”、“可读写（数据）段”会被**映射**到进程的**虚拟地址空间**的“只读代码段”、“可读写数据段” ——> 这个映射关系就是由“程序头表”记录
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310160327076.png)


#### <3>可执行文件的存储器映像
- 进程的“虚拟地址空间”布局大体固定但地址可随机化，而其中的“只读代码段”、“可读写数据段”则是从ELF可执行文件中装入
- 注意：程序加载过程中，实际上并没有真正从磁盘上加载代码和数据到内存，而是仅仅创建了只读代码段和可读写数据段对应“VMA”、“页表” ——> 只有在执行代码过程中发生了“缺页”异常时，才真正从磁盘加载代码和数据到主存。
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310160912000.png)


#### <4>可执行文件的装入与执行全过程
##### 0）映射关系
- **虚拟地址空间** ——> **ELF可执行文件空间** （VMA：虚拟内存区域）
- **虚拟地址空间** ——>  **物理地址（内存空间）**（页表）
- 进程中指令、数据的地址，都是**虚拟地址**，访问时，会通过**页表**获取其映射的**物理地址（内存地址）**，当发现没有相应映射时，就会发生**缺页**异常，需要从磁盘中加载对应的指令/数据，那从磁盘的哪里加载呢？ ——> 前面我们说过，虚拟地址空间的“只读代码段”、“可读写数据段”都要从ELF可执行文件中加载，所以会访问内存中的VMA，通过“虚拟地址空间”获取其映射的“ELF可执行文件空间”，进而从对应的ELF可执行文件中加载指令/数据

##### 1）全景概览
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310162308051.png)
- 可执行文件载入到运行过程：
    - **1.解析ELF文件头**：从“ELF头”中解析出“起始虚拟地址”，存入到“程序计数器PC”中；从“程序头表”中解析出“ELF文件空间”与“虚拟地址空间”的映射（“只读代码段”、“可读写数据段”）
    - **2.创建虚拟地址空间**：创建“VMA”，记录“虚拟地址空间”到“ELF文件空间”的映射关系；创建“页表”，记录“物理空间（内存空间）”到“虚拟地址空间”的映射关系
        - 注意：此时只是建立了映射关系，但还没有将“代码”、“数据”加载到内存中
    - 3.**执行指令**：
        - **取指**：“程序计数器PC”中记录了下一条指令的地址，初始时已经被设置为了“起始虚拟地址”，程序执行时，会访问PC中记录指令地址获取指令 ——> 因为记录的是“虚拟地址”，所以会访问“页表”（有快表就先访问快表）获取其对应的“物理地址”，如果不存在，则发生**缺页异常**，进而访问“VMA”，获取“虚拟地址”对应的“ELF可执行文件地址”，从磁盘文件将指令加载到内存中，然后填写页表映射；
        - **译码、执行**
        - **访存**：译码后的指令中的数据地址也是“虚拟地址”，“获取内存数据”、“将执行结果写回内存”都要经历类似“取指”的过程
        - **写回**：把指令执行的最终结果写回CPU的寄存器（或特殊存储位置），供后续指令使用

##### 2）解析ELF头文件
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310162411428.png)

##### 3）创建虚拟地址空间——VMA 与页表
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310164037432.png)![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310164055003.png)

##### 4）取指与取数据——CPU 如何翻译虚拟地址
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260310164134235.png)

### （2）Linux 最多可运行多少进程？
```bash
面试官您好，这个问题需要从多个层面来回答：

【理论上限】
- 32位系统：PID最大32768，约3万个进程
- 64位系统：PID可配置到4194304，约400万个进程

【实际限制因素】
1. 系统级：pid_max、threads-max
2. 用户级：ulimit -u 配置的 nproc
3. 硬件级：物理内存（每个进程至少占用数百KB）

【关键点】
- Linux中进程和线程共享同一ID空间
- 实际可运行数 = min(所有限制因素)
- 普通系统通常运行几百个进程，服务器可达数千个
```
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305111210403.png)
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305111231939.png)


### （3）Linux进程最大地址空间
- 进程的地址空间分为： **虚拟地址空间** 和 **物理地址空间**
- 我们编写代码时，使用的内存地址是“虚拟地址空间”，通过页表映射到“物理地址空间”，代码执行时，会通过“页表”访问数据，


# 三、网络与IO
## 1.介绍一下epoll多路复用

### （0）一句面试版
```text
epoll 是 Linux 下高性能 I/O 多路复用机制。它通过 epoll_create(1)/epoll_create1 创建实例，用 epoll_ctl 管理关注的 fd 及事件，用 epoll_wait 获取就绪事件。底层是“红黑树管理关注集合 + 就绪链表管理活跃 fd + 回调通知”，避免了 select/poll 每次轮询全量遍历。触发模式有 LT（默认，易用）和 ET（高效但必须非阻塞并读到 EAGAIN）
```

### （1）什么是epoll？
- epoll 是 Linux 内核提供的一种**高效的 I/O 多路复用机制**，用于监控多个文件描述符（fd）上的 I/O 事件。
- 它是 select 和 poll 的增强版本，能够在大量并发连接中高效工作。

### （2）epoll的核心API（三个系统调用）
```cpp
// 1.epoll把用户关心的文件描述符上的事件放在一个“内核事件表”中
//   - epoll_create 用来创建唯一标识epoll“内核事件表”的文件描述符
int epoll_create(int size);  // size 参数已被忽略，但必须 > 0

// 2.操作epoll的内核事件表，将用户关心的文件描述符从内核事件表中“注册/修改/删除”
int epoll_ctl(int epfd, int op, int fd, struct epoll_event *event);
//   op: EPOLL_CTL_ADD / EPOLL_CTL_MOD / EPOLL_CTL_DEL

// 3. 在超时时间内等待内核事件表上的事件发生，函数成功时返回就绪的文件描述符个数
//  - 出参 epoll_event数组中存储了发生事件的epoll_event,从中可提取fd
int epoll_wait(int epfd, struct epoll_event *events, int maxevents, int timeout);
```
- epoll_wait 不一定阻塞
    - timeout = -1：一直阻塞
    - timeout = 0：立即返回（非阻塞轮询）
    - timeout > 0：最多阻塞 timeout 毫秒

### （3）epoll的工作流程
- 通过epoll_create创建唯一标识epoll内核事件表的文件描述符
- 创建要监听的文件描述符的epoll_event进行绑定，并设置监听的事件（读事件/写事件/异常事件等）
- 通过epoll_ctl向epoll内核事件表中注册要监听的文件描述符对应的epoll_event
- 调用epoll_wait等待内核事件表上事件的发生，根据返回的“就绪文件描述符”数量，从出参epoll_event数组中取出文件描述符一次进行处理

### （4）epoll的两种工作模式
#### <1>LT（Level Triggered）— 水平触发（默认）
```cpp
// 只要缓冲区有数据可读，每次 epoll_wait 都会通知
event.events = EPOLLIN;  // 默认 LT 模式
```
- 缓冲区**有数据就一直通知**
- 可以不一次性读完，下次还会通知
- **编程简单，不容易丢数据**
- **性能较低**

#### <2>ET（Edge Triggered）— 边缘触发
```cpp
// 只有状态变化时才通知（从无数据 → 有数据）
event.events = EPOLLIN | EPOLLET;
```
- 只在**状态变化时通知一次**
- **必须一次性读完所有数据（必须循环读/写（以及 accept）直到返回 EAGAIN，否则可能丢后续通知。）**（循环读到 EAGAIN）
- **必须配合非阻塞 I/O** 使用
- **性能更高**，减少了 epoll_wait 的调用次数
```cpp
// ET 模式下的典型读取方式
while (1) {
    int n = read(fd, buf, sizeof(buf));
    if (n == -1) {
        if (errno == EAGAIN) break;  // 数据读完了
        perror("read error");
        break;
    }
    if (n == 0) { /* 连接关闭 */ break; }
    // 处理数据...
}
```
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260309164607973.png)

### （5）epoll的底层原理
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260309163752455.png)

### （6）epoll vs select vs poll
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260309164040822.png)

### （7）面试高频追问
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260309164127302.png)![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260309164546925.png)


## 2.介绍一下Reactor模型
### （1）Reactor模型是什么
- **Reactor 是一种事件驱动的网络编程模型。**
    - **事件驱动**：程序不是按固定顺序一直往下执行，而是先“等事件”，事件发生后再执行对应处理逻辑。
核心思想是：
    - 用 **select/poll/epoll** 统一监听大量连接的 I/O 事件
    - 哪个**连接就绪**，就把事件分发给对应的 工作线程 去处理

### （2）常见三种线程模型
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260309171827931.png)

### （3）Reactor的工作流程（以epoll为例）
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260309171138071.png)

### （4）与Proactor的区别
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260309171243564.png)
- Reactor：通知你“可以读/写了”，read/write 由应用自己做
- Proactor：通知你“读/写已经完成了”，I/O 操作由内核/系统异步完成后回调

# 四、C++相关问题

## 1.C++各类型数据的存储位置
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305111610059.png)
```cpp
高地址
┌─────────────────────────────────────────────────┐
│                内核空间                          │
├─────────────────────────────────────────────────┤
│              栈 (Stack) ↓                       │
│  • 局部变量、函数参数                            │
│  • 局部对象（含 vptr）                          │
│  • const 局部变量                               │
├─────────────────────────────────────────────────┤
│            （未分配区域）                        │
├─────────────────────────────────────────────────┤
│              堆 (Heap) ↑                        │
│  • new/malloc 分配的内存                        │
│  • 动态对象（含 vptr）                          │
├─────────────────────────────────────────────────┤
│              .bss 段                            │
│  • 未初始化的全局/静态变量                       │
├─────────────────────────────────────────────────┤
│              .data 段                           │
│  • 已初始化的全局/静态变量                       │
│  • 静态成员变量                                 │
├─────────────────────────────────────────────────┤
│              .rodata 段                         │
│  • 字符串常量                                   │
│  • const 全局变量                               │
│  • ⭐ 虚函数表 (vtable)                         │
│  • typeinfo                                    │
├─────────────────────────────────────────────────┤
│              .text 段                           │
│  • 函数代码（包括虚函数）                        │
│  • 成员函数代码                                 │
├─────────────────────────────────────────────────┤
│              保留区                              │
└─────────────────────────────────────────────────┘
低地址
```

## 2.C++类的三大特性
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305112825260.png)
**💡 三者关系**
```bash
封装 → 是基础（类的基本概念）
继承 → 建立在封装之上（复用封装好的类）
多态 → 建立在继承之上（通过继承+虚函数实现）
```

### （1）封装
- **将数据和操作数据的方法绑定在一起，对外隐藏实现细节，只暴露接口。**
- **优点**：
	- 保护数据安全（不能随意修改 balance）
	- 降低耦合度
	- 便于修改内部实现而不影响外部

### （2）继承
- **子类继承父类的属性和方法，实现代码复用。**
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305113157460.png)

#### <1>多重继承时的 vtable
```cpp
class A { virtual void fa(); };
class B { virtual void fb(); };
class C : public A, public B { };  // C 有两个 vptr！
```

#### <2>多继承需要解决的两个关键问题
##### 1）名冲突
- 在多继承中，当多个基类中包含同名的成员时，在派生类中就会出现**名冲突**
- 解决方法：**基类名受限访问**
```cpp
#include <iostream>
using namespace std;

class A {
public:
    void func() { cout << "A::func()" << endl; }
    int value = 10;
};

class B {
public:
    void func() { cout << "B::func()" << endl; }
    int value = 20;
};

class C : public A, public B {
    // 继承了 A::func()、A::value
    // 继承了 B::func()、B::value
    // 名冲突！
};

int main() {
    C c;
    
    // ❌ 编译错误：二义性
    // c.func();      // error: ambiguous
    // c.value = 5;   // error: ambiguous
    
    return 0;
}
```
```cpp
#include <iostream>
using namespace std;

class A {
public:
    void func() { cout << "A::func()" << endl; }
    int value = 10;
};

class B {
public:
    void func() { cout << "B::func()" << endl; }
    int value = 20;
};

class C : public A, public B {
public:
    // 方法1：在派生类中用作用域解析符调用
    void callBoth() {
        A::func();  // 明确调用 A 的版本
        B::func();  // 明确调用 B 的版本
    }
    
    // 方法2：在派生类中定义自己的版本（隐藏基类版本）
    void func() {
        cout << "C::func() - 调用自己的实现" << endl;
        // 如需调用基类，仍可用 A::func() 或 B::func()
    }
};

int main() {
    C c;
    
    // ✅ 使用基类名受限访问
    c.A::func();      // 输出: A::func()
    c.B::func();      // 输出: B::func()
    
    c.A::value = 100;
    c.B::value = 200;
    cout << "A::value = " << c.A::value << endl;  // 100
    cout << "B::value = " << c.B::value << endl;  // 200
    
    // ✅ 调用派生类自己的方法
    c.func();         // 输出: C::func() - 调用自己的实现
    c.callBoth();     // 输出: A::func()  B::func()
    
    return 0;
}
```

##### 2）重复继承（菱形继承） —— 虚基类
- 在多继承中，如果继承的类有公共的基类，则会出现**重复继承** ——> 公共基类中的数据成员在多继承的派生类中有多个拷贝
- 解决方案：**虚基类**
```cpp
class A{
	int x;
	......
};

class B: public A {......};
class C: public A {......};
class D: public B,public C{
	......
	void f(){
		...B::x... // 访问从B继承来的x
		...C::x... // 访问从C继承来的x
	}
};
......
D d;	// d有两个x
```
- 通常情况下，我们需要把这两个x合并为一个x，此时，应把类A定义为类B和类C的**虚基类**，这样定义后，类D就只有一个成员x了
```cpp
class B: virtual public A {......};
class C: virtual public A {......};
class D: public B,public C{
	......
	void f(){
		...x...	// 访问从B和C继承来的公共x
	}
};
......
D d;	// d只有一个x
```


##### 拓展：包含虚基类的类的“两个注意点”
- 虚基类的构造函数由该类的构造函数直接调用
- 虚基类的构造函数优先于非虚基类的构造函数执行
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305133338300.png)

### （3）多态
- **同一接口，不同实现。分为编译时多态和运行时多态。**
	- **编译时多态（静态多态）**：函数重载 & 模板
	- **运行时多态（动态多态）**：通过虚函数实现
- **动态多态**：基类通过virtual定义虚函数后，子类继承该基类并override虚函数，再通过**基类指针/引用** 指向/引用 **子类对象**

### 🎯 面试速答
- **封装**：将数据和方法绑定，通过"访问控制符"隐藏实现细节，只暴露必要接口。
- **继承**：子类继承父类的属性和方法，实现代码复用和层次结构。
- **多态**：同一接口不同实现。编译时多态通过"函数重载/模板"实现；运行时多态通过"虚函数和继承"实现，基类指针可调用派生类方法。


## 3.虚函数相关面试问题
### （1）介绍一下虚函数、虚函数表、虚函数指针，以及三者如何协作实现"虚函数动态绑定"
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305115526861.png)
- **虚函数**： virtual定义的成员函数
- **虚函数表**：对于每一个类，如果它有虚函数（**包括从基类继承来的**），则编译程序将会为其创建一个虚函数表（vtbl），表中记录了该类中所有虚函数的入口地址
	- 虚函数表存储在**只读数据段 .rodata**中
- **虚函数表指针**：当创建一个包含虚函数的类的对象时，在所创建对象的内存空间中有一个隐藏的指针（vptr）指向该对象所属类的虚函数表。
	- **vptr初始化时机**： 在构造函数执行时设置（先指向父类 vtable，再指向子类 vtable）
- **虚函数动态绑定的实现方式**：
	- 基类通过virtual定义成员函数，子类继承该基类后，由于包含虚函数（包括从基类继承来的），故程序会在 **.rodata** 段为该类创建一个**虚函数表**，为该类的所有对象共用。
	- 子类的虚函数表中，最开始存储的是**父类的虚函数的入口地址**，当子类override该虚函数后，虚函数表中对应位置将改为存储**子类override后的虚函数的入口地址**
	- 当通过**引用/指针**访问类的虚函数时，将会利用它们引用或指向的对象内存空间中的**虚函数表指针**从**虚函数表**中获取相应虚函数的入口地址来调用相应的函数

![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305120020774.png)

### （2）vptr初始化时机
- 在“构造函数”的**初始化列表之后、函数体之前**被隐式设置。
- **先指向父类 vtable，再指向子类 vtable**

#### <1>为什么在“初始化列表之后、函数体之前”
- 在构造函数中可能调用虚函数
```cpp
class Base {
public:
    Base() { 
        cout << "Base 构造" << endl;
        this->show();  // 调用哪个版本？
    }
    virtual void show() { cout << "Base::show" << endl; }
};

class Derived : public Base {
public:
    Derived() { 
        cout << "Derived 构造" << endl;
        this->show();  // 调用哪个版本？
    }
    void show() override { cout << "Derived::show" << endl; }
};

Derived d;
```

#### <2>为什么“先指向父类 vtable，再指向子类 vtable”
- 因为构造 Derived 对象（子类对象）时，先执行 Base 构造函数（此时 vptr 指向 Base::vtable），再执行 Derived 构造函数（vptr 更新为 Derived::vtable）。
```cpp
创建 Derived 对象 d：

时间线 ──────────────────────────────────────────────────────────►

     ┌─────────────────────────────────────────────────────────┐
     │ 1. 分配内存                                             │
     │    vptr = 未初始化                                      │
     └─────────────────────────────────────────────────────────┘
                              ↓
     ┌─────────────────────────────────────────────────────────┐
     │ 2. 进入 Base::Base()                                    │
     │    vptr = &Base::vtable  ←── 设置为父类的 vtable        │
     │    执行 Base 构造函数体                                  │
     │    调用 this->show() → Base::show() ✓                  │
     └─────────────────────────────────────────────────────────┘
                              ↓
     ┌─────────────────────────────────────────────────────────┐
     │ 3. 进入 Derived::Derived()                              │
     │    vptr = &Derived::vtable  ←── 更新为子类的 vtable     │
     │    执行 Derived 构造函数体                               │
     │    调用 this->show() → Derived::show() ✓               │
     └─────────────────────────────────────────────────────────┘
                              ↓
     ┌─────────────────────────────────────────────────────────┐
     │ 4. 构造完成                                             │
     │    vptr = &Derived::vtable （最终状态）                  │
     └─────────────────────────────────────────────────────────┘
```

### （3）为什么析构函数往往是虚函数，而构造函数不是？
#### <1>析构函数往往是虚函数
##### 1）delete 操作的本质
```cpp
delete p;

// 等价于：
p->~???();           // 第一步：调用析构函数（调用哪个？）
operator delete(p);  // 第二步：释放内存
```
- **非虚析构**：delete p 时，编译器根据指针类型 Base\* 在编译期直接生成调用 Base::~Base() 的代码，不会查虚函数表。
- **虚析构**：析构函数地址存入 vtable，delete p 时通过 vptr → vtable → 析构函数地址，运行时找到对象实际类型的析构函数（Derived::~Derived()）进行调用，然后自动调用父类的析构函数。
- 这就是为什么**有继承关系且可能通过基类指针删除对象时，往往使用虚析构函数**
```cpp
【非虚析构】                           【虚析构】

delete p;                              delete p;
    │                                      │
    ▼                                      ▼
编译期确定                              运行期查表
调用 Base::~Base()                          │
    │                                      ▼
    │                               ┌──────────────────┐
    │                               │ 1. 取 vptr       │
    │                               │ 2. vptr → vtable │
    │                               │ 3. vtable[0]     │
    │                               │    → ~Derived    │
    │                               └──────────────────┘
    │                                      │
    ▼                                      ▼
~Base() 执行                         ~Derived() 执行
    │                                      │
    ▼                                      ▼
operator delete(p)                   然后自动调用 ~Base()
                                           │
                                           ▼
                                    operator delete(p)

结果：Derived::data 泄漏！           结果：正确释放所有资源 ✓
```

##### 拓展1：子类析构后自动调用父类析构的原理
- **📌 关键澄清**：**这个行为与"虚"无关！** 无论析构函数是否为虚函数，子类析构后都会自动调用父类析构。
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305130441650.png)

##### 拓展2：子类析构函数的自动重写机制
```cpp
class Base {
public:
    virtual ~Base() { cout << "~Base" << endl; }
};

class Derived : public Base {
public:
    ~Derived() { cout << "~Derived" << endl; }  // 自动成为虚函数，自动重写
    // 等价于：virtual ~Derived() override { ... }
};
```
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305125922121.png)
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305125947121.png)

#### <2>构造函数不能使虚函数
- 在创建对象时，会自动调用构造函数，如果将构造函数设置为虚函数，则会通过虚函数表指针去虚函数表中获取函数入口地址并执行，但一个对象的**虚函数表指针**是在构造函数中进行初始化的（指向虚函数表），也就意味着将用“未初始化的虚函数表指针（还未执行对应虚函数表）”去虚造构造入口地址，进而导致错误

### （4）虚函数、虚函数指针、虚函数表分别存储在什么区域？
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260305131022089.png)

### （5）纯虚函数 & 抽象类
- **抽象类**：含有纯虚函数的类
	- **抽象类不能实例化，子类必须实现纯虚函数**
```cpp
class Shape {
public:
    virtual void draw() = 0;  // 纯虚函数
    virtual ~Shape() = default;
};
// Shape 不能实例化，子类必须实现 draw()
```

### （6）虚函数的性能开销
```bash
虚函数调用比普通函数多一次内存寻址（vptr → vtable → func），约 10-20% 性能差异。
现代 CPU 分支预测可缓解部分开销。
```

## 4.类之间的聚合/组合关系
- **聚合关系**：类似于一个公司，公司员工聚在公司一起工作，公司解散了，员工仍存在，可以去其它公司，不依赖于公司而存在,可以独立存在
- **组合关系**：类似于人与器官，器官依赖于人这个整体，人死了，器官也就死了
```cpp
class A{
	......
};

class B{	// B与A是聚合关系
	A *pm;	// 指向成员对象
public:
	B(A *p) {pm=p;}		// 外部创建，然后传入
	~B() {pm=nullptr;}	// 由外部释放资源，这里只是不再指向传入的对象
};

class C{
	A *pm;
public:
	C() {pm=new A;}		// 由内部创建
	~C(){delete pm;}	// 成员对象随组合类对象消亡
};
......
A *pa=new A;		// 创建一个A类对象
B *pb=new B(pa);	// 创建一个聚合类对象，其成员对象由“外部传入”
C *pc=new C();		// 创建一个组合类对象，其成员对象在组合类对象“内部创建”
......
delete pb;		// 聚合类对象消亡了，其成员对象不再指向pa，且pa并没有消亡，仍可以在其它地方使用
delete pc;		// 组合类对象与其成员对象都消亡
delete pa；		// 聚合类对象原来的成员对象消亡
```

## 5.C++关键字
### （1）final 和 override 关键字
```cpp
class Base {
    virtual void func() final;  // 禁止子类重写
};

class Derived : public Base final {  // 禁止继续继承
    void func() override;  // 编译器检查是否真的重写
};
```