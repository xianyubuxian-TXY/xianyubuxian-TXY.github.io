---
title: 兑换零钱(一)
date: 2026-02-06 23:31:46
tags:
	- oj
	- 笔试
categories:
	- 编程题
	- 动态规划
	- 背包问题
---

# 1.题目描述
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260206233414901.png)
- **完全背包问题**

# 2.解题思路
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260206233651959.png)![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260206233729694.png)

# 3.代码实现
```cpp
#include <climits>
class Solution {
public:
    /**
     * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
     *
     * 最少货币数
     * @param arr int整型vector the array
     * @param aim int整型 the target
     * @return int整型
     */
    int minMoney(vector<int>& arr, int aim) {
        // write code here
        int len=arr.size();
        vector<int> dp(aim+1,INT_MAX);  // dp[k]：组成金额k的最少货币数
        dp[0]=0;
        for(int i=0;i<=aim;i++){    // 遍历所有金额
            for(int j=0;j<len;j++){ // 最后一张使用第j种面值的货币
                if(i<arr[j]) continue;
                int money=arr[j];
                if(dp[i-money]!=INT_MAX){   // 一定要检查，否则溢出变为“负数”，后面就全错了
                    dp[i]=min(dp[i],dp[i-money]+1);
                }

            }
        }

        return dp[aim]==INT_MAX?-1:dp[aim];
    }
};
```
