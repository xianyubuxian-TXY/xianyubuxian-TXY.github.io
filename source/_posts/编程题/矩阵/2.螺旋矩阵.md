---
title: 2.螺旋矩阵
date: 2026-04-01 14:43:45
tags:
	- oj
	- 笔试
categories:
	- 编程题
	- 矩阵
---

# 1.题目描述
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260401144420395.png)

# 2.题目解析
![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260401144457635.png)![](https://cdn.jsdelivr.net/gh/xianyubuxian-TXY/hexo-images/images/20260401144549107.png)

# 3.代码实现
```cpp
class Solution {
public:
    vector<int> spiralOrder(vector<vector<int>>& matrix) {
        int rowSize=matrix.size();
        int colSize=matrix[0].size();
        int left=0,right=colSize-1;
        int top=0,bottom=rowSize-1;

        vector<int> res;
        while(left<=right && top<=bottom){
            // 1.从左到右
            for(int i=left;i<=right;i++){
                res.push_back(matrix[top][i]);
            }
            top++;
            if(top>bottom) break;   // 每次范围变化，都判断一次

            // 2.从上到下
            for(int i=top;i<=bottom;i++){
                res.push_back(matrix[i][right]);
            }
            right--;
            if(right<left) break;   // 每次范围变化，都判断一次

            // 3.从右到左
            for(int i=right;i>=left;i--){
                res.push_back(matrix[bottom][i]);
            }
            bottom--;
            if(bottom<top) break;   // 每次范围变化，都判断一次

            // 4.从下到上
            for(int i=bottom;i>=top;i--){
                res.push_back(matrix[i][left]);
            }
            left++;
            if(left>right) break;   // 每次范围变化，都判断一次
        }

        return res;
    }
};
```